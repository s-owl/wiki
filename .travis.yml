language: node_js
dist: trusty
addons:
  ssh_known_hosts:
   - github.com
   - 192.30.253.113
before_install:
 - openssl aes-256-cbc -K $encrypted_01897fd6d767_key -iv $encrypted_01897fd6d767_iv
   -in deploy_rsa.enc -out deploy_rsa -d
install:
 - npm install -g gitbook-cli
script:
 - git --version
 - echo "Importing SSH Key"
 - eval "$(ssh-agent -s)"
 - chmod 600 $TRAVIS_BUILD_DIR/deploy_rsa
 - ssh-add $TRAVIS_BUILD_DIR/deploy_rsa
 - echo "Configuring Git"
 - git config --global user.email "we.sss.you@gmail.com"
 - git config --global user.name "SSS Wiki Bot (via TravisCI)"
 - echo "Fetching Repo"
 - mkdir _book
 - cd _book
 - git init
 - git remote add origin git@github.com:skhu-sss/skhu-sss.github.hub.io.git
 - git checkout --orphan master
 - echo "Building Pages"
 - cd ..
 - gitbook build
 - cd _book
 - echo "Pushing Updates"
 - git remote set-url origin git@github.com:skhu-sss/skhu-sss.github.io.git
 - git add .
 - git pull origin master
 - git pull origin src
 - git commit -m "Updated Contents - via Travis CI"
 - git push origin master
